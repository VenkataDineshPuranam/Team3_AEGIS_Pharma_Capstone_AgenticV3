# architecture

C4 model, DDD bounded contexts, agentic architecture (LangGraph), ontology/semantic layer.

- [Low-level Azure-style diagram](low-level/aegis_low_level.html) — nested subscription / VNet / subnet view of the running repo (Control Center, six LangGraphs, in-process tools, Neo4j / Redis / SQLite, ADR-009 Azure bindings, chaos-drill injectors). Open the HTML in a browser.
